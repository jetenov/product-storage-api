# Code generated by "scratch generate config". DO NOT EDIT.
realtimeConfig:
  circuit_breakers:
    usage: CircuitBreaker rules for grpc clients
    group: scratch
    value: '{"__default__":{"disabled":false,"opener":{"err_percent":20}}}'
    type: string
  clientside_balancing_diagnostics:
    usage: Settings for clientside balancing diagnostics
    group: scratch
    value: ""
    type: string
  gc_memory_ballast_size:
    usage: Memory ballast size for reduce GC call frequency (restart required; KB/MB/GB)
    group: scratch
    value: 0MB
    type: string
  gogc:
    usage: Sets the GC target percentage
    group: scratch
    value: 100
    type: int
  gomaxprocs:
    usage: Sets GOMAXPROCS value
    group: scratch
    value: auto
    type: string
  grpc_call_options:
    usage: gRPC call option settings
    group: scratch
    value: ""
    type: string
  grpc_server_initial_conn_window_size:
    usage: 'Sets window size for a connection. The lower bound for window size is
      64K and any value smaller than that will be ignored (restart required; size
      in BYTES; default: 64KB)'
    group: scratch
    value: 65535
    type: int
  grpc_server_initial_window_size:
    usage: 'Sets window size for stream. The lower bound for window size is 64K and
      any value smaller than that will be ignored (restart required; size in BYTES;
      default: 64KB)'
    group: scratch
    value: 65535
    type: int
  grpc_server_max_concurrent_streams:
    usage: 'will apply a limit on the number of concurrent streams to each ServerTransport
      (restart required; default: 0)'
    group: scratch
    value: 0
    type: uint
  grpc_server_max_recv_msg_size:
    usage: 'Sets the max message size in bytes the server can receive (restart required;
      size in BYTES; default: 4MB)'
    group: scratch
    value: 4194304
    type: int
  grpc_server_max_send_msg_size:
    usage: 'Sets the max message size in bytes the server can send (restart required;
      size in BYTES; default: math.MaxInt32)'
    group: scratch
    value: 2147483647
    type: int
  grpc_server_read_buffer_size:
    usage: 'Determines how much data can be read at most for one read syscall (restart
      required; size in BYTES; default: 32KB)'
    group: scratch
    value: 32768
    type: int
  grpc_server_stream_worker_count:
    usage: Number of worker goroutines to process incoming streams (restart required)
    group: scratch
    value: 0
    type: uint
  grpc_server_write_buffer_size:
    usage: 'Determines how much data can be batched before doing a write on the wire
      (restart required; size in BYTES; default: 32KB)'
    group: scratch
    value: 32768
    type: int
  gzip_limit_request_size_enabled:
    usage: Enable limits for gzip request's size
    group: scratch
    value: 0
    type: int64
  hedged_requests:
    usage: Reissue requests policies
    group: scratch
    value: ""
    type: string
  metrics_db_response_time_enabled:
    usage: Enable database (pgsql/mssql) response time metric
    group: scratch
    value: true
    type: bool
  metrics_db_response_time_query_label_enabled:
    usage: Enable query label in db (pgsql/mssql) response time metric
    group: scratch
    value: true
    type: bool
  metrics_grpc_call_options_enabled:
    usage: Enable gRPC call options metrics
    group: scratch
    value: true
    type: bool
  metrics_grpc_header_size_enabled:
    usage: Enable gRPC header size metrics
    group: scratch
    value: false
    type: bool
  metrics_grpc_payload_size_enabled:
    usage: Enable gRPC payload size metrics
    group: scratch
    value: false
    type: bool
  metrics_request_client_name_enabled:
    usage: Enable HTTP/gRPC client_name label in request metrics
    group: scratch
    value: false
    type: bool
  overload_ammo_collector_agent_enabled:
    usage: Enable overload ammo collector agent
    group: scratch
    value: false
    type: bool
  rate_limits:
    usage: Rate limit rules for incoming traffic
    group: scratch
    value: ""
    type: string
